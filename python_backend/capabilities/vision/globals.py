
# Vision Service Global State
# Used for shared logical state, similar to STT/TTS

import logging

logger = logging.getLogger("VisionGlobals")

# No complex global state needed for now, but keeping the pattern
